# Урок 1.2: Переменные — инвентарь для данных

*Где программа хранит свои вещи.*

---

## 🎣 Hook

У тебя в Minecraft есть инвентарь. 36 слотов. В каждый можно положить что-то одно: меч, еду, блоки.

Теперь представь: ты строишь дом. Тебе нужно запомнить его размеры — ширина 10, длина 15, высота 5. Где хранить эти числа?

В программировании для этого есть **переменные**. Это как слоты инвентаря, но для данных. Ты кладёшь туда значение, даёшь ему имя и используешь когда нужно.

---

## 🧠 Concept: Переменные

**Переменная** — это именованная ячейка памяти. У неё есть:
- **Имя** — как подпись на сундуке
- **Значение** — что лежит внутри
- **Тип** — какого рода данные

```python
# Создаём переменные
ширина = 10
длина = 15
высота = 5
материал = "камень"
есть_крыша = True
```

Разберём:

| Имя | Значение | Тип | Minecraft-аналогия |
|-----|----------|-----|-------------------|
| `ширина` | 10 | число (int) | Стак из 10 блоков |
| `длина` | 15 | число (int) | Стак из 15 блоков |
| `высота` | 5 | число (int) | Стак из 5 блоков |
| `материал` | "камень" | текст (string) | Табличка с надписью |
| `есть_крыша` | True | логический (bool) | Рычаг: вкл/выкл |

### Основные типы данных

- **int** (целое число) — `64`, `0`, `-5`. Количество предметов в стаке.
- **float** (дробное число) — `3.14`, `0.5`. Уровень здоровья: 18.5 HP.
- **string** (текст) — `"Привет"`, `"diamond_sword"`. Название предмета.
- **bool** (логический) — `True` или `False`. Рычаг: включён или выключен.

### Переменные можно менять

Это же *пере*менные. Значение можно обновить:

```python
здоровье = 20        # Полное HP
здоровье = 20 - 7    # Крипер взорвался рядом
здоровье = 13        # Теперь 13 HP
```

И использовать в вычислениях:

```python
ширина = 10
длина = 15
площадь = ширина * длина    # 150
периметр = 2 * (ширина + длина)  # 50 блоков стены
```

Зачем это круто? Потому что если ты решишь изменить размер дома, тебе нужно поменять ОДНО число, а не переписывать всю программу.

```python
ширина = 20    # Поменял одну строку — и весь дом стал шире
```

---

## 🔧 Practice

**Задание 1: Калькулятор ресурсов**

Ты строишь прямоугольную стену. Нужно посчитать, сколько блоков понадобится.

```python
# Допиши код
длина_стены = 10
высота_стены = 4
блоков_нужно = ???    # Сколько блоков на стену?
```

<details>
<summary>Решение</summary>

```python
блоков_нужно = длина_стены * высота_стены    # 40
```

</details>

**Задание 2: Характеристики моба**

Создай переменные для описания крипера:

```python
# Заполни переменные
имя = ???
здоровье = ???
урон_взрыва = ???
боится_кошек = ???
```

<details>
<summary>Решение</summary>

```python
имя = "Creeper"
здоровье = 20
урон_взрыва = 49    # Максимальный урон на Hard
боится_кошек = True  # Факт из игры
```

</details>

**Задание 3: Обмен значений**

У тебя два слота инвентаря. Нужно поменять их содержимое местами.

```python
слот_1 = "меч"
слот_2 = "щит"
# Поменяй местами, чтобы слот_1 = "щит", слот_2 = "меч"
# Подсказка: тебе понадобится третья переменная
```

<details>
<summary>Подсказка (стоит 10 XP)</summary>

Представь, что у тебя два сундука и один пустой. Чтобы поменять предметы местами, нужно сначала положить один из них во временный сундук.

</details>

<details>
<summary>Решение</summary>

```python
слот_1 = "меч"
слот_2 = "щит"

временный = слот_1     # временный = "меч"
слот_1 = слот_2        # слот_1 = "щит"
слот_2 = временный     # слот_2 = "меч"
```

Это классическая задача на собеседованиях. Да, уже на уроке 2 ты решаешь задачи с собесов.

</details>

---

## 🏗️ Build: Настраиваемый дом

Черепашка строит дом, размеры которого задаёшь ты через переменные.

```python
# Настройки дома
ширина = 6
длина = 8
высота = 4
материал = "дуб"

# Черепашка строит по твоим параметрам
построй_дом(ширина, длина, высота, материал)
```

Поменяй значения переменных и запусти снова. Видишь, как один и тот же код строит разные дома? Вот в этом сила переменных — **код пишешь один раз, а результат меняешь, подставляя другие данные**.

Попробуй:
- Дом 3×3×2 из камня (хижина)
- Дом 20×20×10 из кварца (дворец)
- Дом 1×1×50 из обсидиана (башня безумия)

---

## 👹 Boss Challenge: Калькулятор строителя

**Задание:** Напиши программу, которая по размерам постройки считает:
1. Сколько блоков нужно на **стены** (4 стены, без пола и потолка)
2. Сколько блоков нужно на **пол**
3. Сколько блоков нужно на **потолок**
4. **Общее** количество блоков
5. Сколько **стаков** (1 стак = 64 блока) нужно собрать

```python
ширина = 10
длина = 12
высота = 5

# Твой код здесь
пол = ???
потолок = ???
стены = ???
всего = ???
стаков = ???

print(f"Нужно блоков: {всего}")
print(f"Это {стаков} стаков")
```

Подсказка: стены — это не просто `(ш + д) * 2 * в`. Нужно подумать про углы, чтобы не посчитать блоки дважды.

<details>
<summary>Решение</summary>

```python
ширина = 10
длина = 12
высота = 5

пол = ширина * длина                              # 120
потолок = ширина * длина                           # 120
стены = 2 * (ширина + длина - 2) * (высота - 2)   # Минус пол, потолок и углы
# Или проще: периметр * высота - углы
стены = (2 * ширина * (высота - 2)) + (2 * (длина - 2) * (высота - 2))  # 120
всего = пол + потолок + стены                      # 360
стаков = всего // 64                               # 5 полных стаков
остаток = всего % 64                               # 40 блоков

print(f"Нужно блоков: {всего}")
print(f"Это {стаков} стаков и {остаток} блоков")
```

`//` — это целочисленное деление (без остатка). `%` — остаток от деления. Полезные штуки, запомни.

</details>

---

## 💭 Reflection

**Теперь ты знаешь:**
- ✅ Переменная = именованная ячейка для данных
- ✅ Четыре базовых типа: числа, текст, логический
- ✅ Переменные можно менять и использовать в вычислениях
- ✅ Одна программа + разные переменные = разные результаты

**Где это в реальном мире:**
- Корзина в интернет-магазине — переменная `total_price` обновляется при добавлении товара
- Счётчик подписчиков на YouTube — переменная, которая растёт (или падает)
- Твой ник в игре — строковая переменная, которую задал ты

**Пасхалка:** Знаешь, почему переменную для обмена значений называют `temp`? Это сокращение от temporary (временный). Программисты ленивые — им лень писать полные слова. Привыкай.

**В следующем уроке:** черепашка научится принимать решения. Если крипер — бежим. Если корова — игнорируем. Это называется «условия», и без них не обойтись.

---

*+100 XP за прохождение урока. +75 бонусных XP за Boss Challenge.*
