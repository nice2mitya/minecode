# Урок 1.3: Условия — if creeper then run

*Как научить программу принимать решения.*

---

## 🎣 Hook

Ты идёшь по пещере в Minecraft. Впереди моб. Что делаешь?

- Если это **корова** → игнорируешь
- Если это **зомби** → бьёшь мечом
- Если это **крипер** → БЕЖИШЬ
- Если это **Эндермен** → не смотришь ему в глаза

Ты только что выполнил четыре условия. Твой мозг делает это автоматически: оценивает ситуацию и выбирает действие. Программы работают так же — но им нужно прописать каждое условие явно.

---

## 🧠 Concept: if / else

**Условие** — это развилка в коде. Если что-то правда — делаем одно. Если нет — другое.

```python
моб = "крипер"

if моб == "крипер":
    print("БЕЖИМ!")
else:
    print("Атакуем!")
```

Разберём по кусочкам:

- `if` — «если»
- `моб == "крипер"` — проверка (два знака `=` — это сравнение, один `=` — присваивание)
- `:` — двоеточие после условия (Python так хочет, не спорь)
- Отступ (4 пробела) — то, что выполняется, если условие верно
- `else:` — «иначе» (всё остальное)

### elif — когда вариантов больше двух

```python
моб = "зомби"

if моб == "крипер":
    print("БЕЖИМ!")
elif моб == "зомби":
    print("Бьём мечом")
elif моб == "скелет":
    print("Прячемся за щитом")
elif моб == "эндермен":
    print("Не смотрим в глаза")
else:
    print("Что-то неизвестное. Осторожно.")
```

`elif` — это сокращение от «else if» (иначе если). Можно ставить сколько угодно.

### Операторы сравнения

| Оператор | Значение | Пример | Результат |
|----------|----------|--------|-----------|
| `==` | равно | `5 == 5` | `True` |
| `!=` | не равно | `5 != 3` | `True` |
| `>` | больше | `10 > 5` | `True` |
| `<` | меньше | `3 < 7` | `True` |
| `>=` | больше или равно | `5 >= 5` | `True` |
| `<=` | меньше или равно | `3 <= 2` | `False` |

### Составные условия: and, or, not

Иногда нужно проверить несколько вещей сразу:

```python
здоровье = 5
есть_еда = True
рядом_крипер = False

# И — оба условия должны быть верны
if здоровье < 10 and есть_еда:
    print("Ешь! У тебя мало HP")

# ИЛИ — достаточно одного
if здоровье < 5 or рядом_крипер:
    print("Пора валить!")

# НЕ — переворачивает условие
if not рядом_крипер:
    print("Можно расслабиться")
```

Это как в Minecraft: ты идёшь в пещеру, если у тебя есть **и** кирка, **и** факелы. Или ты не идёшь, если **нет** еды **или** слишком мало HP.

---

## 🔧 Practice

**Задание 1: Классификатор мобов**

Напиши код, который определяет тип моба:

```python
моб = "волк"

if ???:
    тип = "враждебный"
elif ???:
    тип = "нейтральный"
else:
    тип = "дружелюбный"

print(f"{моб} — {тип}")
```

Враждебные: крипер, зомби, скелет.
Нейтральные: волк, эндермен, паук (днём).
Дружелюбные: корова, свинья, курица.

<details>
<summary>Решение</summary>

```python
моб = "волк"

if моб == "крипер" or моб == "зомби" or моб == "скелет":
    тип = "враждебный"
elif моб == "волк" or моб == "эндермен" or моб == "паук":
    тип = "нейтральный"
else:
    тип = "дружелюбный"

print(f"{моб} — {тип}")  # волк — нейтральный
```

Бонус: в будущем мы узнаем про списки, и этот код можно будет сделать гораздо чище.

</details>

**Задание 2: Система дня и ночи**

В Minecraft цикл дня = 24000 тиков. Напиши код, который по номеру тика определяет время:

- 0-6000: утро ☀️
- 6000-12000: день 🌞
- 12000-18000: вечер 🌅
- 18000-24000: ночь 🌙 (мобы спавнятся!)

```python
тик = 14500

# Определи время суток и выведи сообщение
# Если ночь — добавь предупреждение о мобах
```

<details>
<summary>Решение</summary>

```python
тик = 14500

if тик < 6000:
    время = "утро ☀️"
elif тик < 12000:
    время = "день 🌞"
elif тик < 18000:
    время = "вечер 🌅"
else:
    время = "ночь 🌙"

print(f"Сейчас {время}")

if тик >= 18000:
    print("⚠️ Мобы спавнятся! Прячься или дерись!")
```

</details>

**Задание 3: Может ли игрок крафтить?**

Для алмазной кирки нужно: 3 алмаза и 2 палки. Напиши проверку:

```python
алмазы = 2
палки = 5

# Можно ли скрафтить алмазную кирку?
```

<details>
<summary>Решение</summary>

```python
алмазы = 2
палки = 5

if алмазы >= 3 and палки >= 2:
    print("Можно крафтить алмазную кирку!")
elif алмазы < 3:
    print(f"Не хватает алмазов: нужно 3, есть {алмазы}")
elif палки < 2:
    print(f"Не хватает палок: нужно 2, есть {палки}")
```

</details>

---

## 🏗️ Build: Умная черепашка

Помнишь черепашку из урока 1? Она тупо шла вперёд. Теперь она станет умнее.

Черепашка двигается по миру и встречает разные блоки. Она умеет их определять с помощью функции `что_впереди()`.

```python
блок = что_впереди()

if блок == "воздух":
    вперёд()
elif блок == "руда":
    копай()
    вперёд()
elif блок == "лава":
    влево()
    вперёд()
    вправо()
    вперёд()
    вправо()
    вперёд()
    влево()
elif блок == "бедрок":
    print("Дальше нельзя")
    развернись()
```

Запусти в виртуальном мире. Черепашка теперь **реагирует** на окружение, а не просто слепо выполняет команды. Это уже настоящий AI (ну, почти).

---

## 👹 Boss Challenge: Боевой робот

**Задание:** Запрограммируй черепашку-бойца. Она стоит на арене и встречает мобов. Для каждого моба нужна своя стратегия:

**Входные данные:**
```python
моб = ???             # Какой моб
расстояние = ???      # Сколько блоков до моба
здоровье = ???        # HP черепашки
есть_щит = ???        # True/False
```

**Правила:**
1. Если здоровье < 5 — всегда убегай, независимо от моба
2. Если крипер и расстояние < 3 — убегай (взрыв!)
3. Если крипер и расстояние >= 3 — стреляй из лука
4. Если зомби — атакуй мечом
5. Если скелет и есть щит — блокируй и подходи
6. Если скелет и нет щита — зигзагом подбегай
7. Если эндермен — не двигайся (он телепортируется)

Напиши код, который обрабатывает ВСЕ эти случаи.

<details>
<summary>Подсказка</summary>

Начни с проверки здоровья — она самая приоритетная. Потом проверяй тип моба. Внутри каждого типа — дополнительные условия.

</details>

<details>
<summary>Решение</summary>

```python
моб = "крипер"
расстояние = 2
здоровье = 12
есть_щит = True

# Сначала проверяем критическое состояние
if здоровье < 5:
    print("HP мало! Отступаем!")
    назад()
    назад()
    назад()

# Потом — тип моба
elif моб == "крипер":
    if расстояние < 3:
        print("Крипер близко! БЕЖИМ!")
        развернись()
        вперёд()
        вперёд()
        вперёд()
    else:
        print("Крипер далеко. Стреляем из лука")
        стреляй()

elif моб == "зомби":
    print("Зомби! Атакуем мечом")
    вперёд()
    атакуй()

elif моб == "скелет":
    if есть_щит:
        print("Скелет! Щитом блокируем и подходим")
        щит()
        вперёд()
        вперёд()
        атакуй()
    else:
        print("Скелет! Зигзагом!")
        влево()
        вперёд()
        вправо()
        вперёд()
        вправо()
        вперёд()
        атакуй()

elif моб == "эндермен":
    print("Эндермен! Замираем. Не смотрим.")
    # Ничего не делаем

else:
    print(f"Неизвестный моб: {моб}. Осторожно!")
```

Обрати внимание на **вложенные условия** — `if` внутри `elif`. Это как дерево решений: сначала крупные ветки, потом мелкие.

</details>

---

## 💭 Reflection

**Теперь ты знаешь:**
- ✅ `if/elif/else` — программа может принимать решения
- ✅ Операторы сравнения: `==`, `!=`, `>`, `<`, `>=`, `<=`
- ✅ `and`, `or`, `not` — составные условия
- ✅ Вложенные условия — дерево решений

**Где это в реальном мире:**
- Спам-фильтр почты: if содержит «вы выиграли миллион» → спам
- Автоматические двери: if датчик движения → открыть
- Светофор: if красный → стой, elif зелёный → иди
- Рекомендации Netflix: if ты смотрел ужасы and оценка > 4 → покажем ещё ужасов

**Fun fact:** Вся логика любого компьютера сводится к двум операциям: сравнение (if) и переход (goto). Буквально всё. Игры, нейросети, операционные системы — просто очень много `if`.

**В следующем уроке:** мы наконец избавимся от копипасты. Помнишь, как мы писали `вперёд()` пять раз подряд? Циклы решат эту проблему навсегда.

---

*+100 XP за прохождение урока. +100 бонусных XP за Boss Challenge (он был непростой).*
